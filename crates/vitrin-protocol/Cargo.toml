[package]
name = "vitrin-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Vitrin OS wire protocol: generated message types + codec. No I/O, no sockets -- pure data and encode/decode."
publish = false

[dependencies]

[dev-dependencies]
proptest = "1"
